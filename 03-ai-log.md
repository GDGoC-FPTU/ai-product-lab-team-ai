# 03 - AI Log

Trong buổi học Lab 02, tôi dùng AI như một người đồng hành để suy nghĩ thay cho việc chỉ hỏi đáp đơn thuần. AI giúp tôi brainstorm nhanh các bài toán vận hành có thể áp dụng cho Vin Smart Future, đặc biệt là các pain point lặp lại hoặc tốn thời gian ở Xanh SM, Vinhomes, Vinmec và VinFast. Nhờ AI, tôi có thể chuyển từ một ý tưởng chung chung sang danh sách bài toán cụ thể hơn, rồi chọn ra các bài toán có workflow rõ, có bottleneck đo được và có khả năng mô tả thành quick card. Ngoài ra, AI còn hỗ trợ tôi viết prompt cho `prompt_prototype.py`, gợi ý cách đặt ranh giới an toàn, cách định dạng output và cách tạo các test adversarial để kiểm tra xem mô hình có bị dụ vượt ranh giới hay không.

Tuy nhiên, AI cũng có lúc trả lời chưa sát với thực tế. Một điểm sai lệch tôi gặp là AI có xu hướng đề xuất giải pháp quá “tham vọng” hoặc quá phức tạp, trong khi nhiều bài toán của lab thực ra có thể được giải quyết tốt hơn bằng rule-based code hoặc một workflow đơn giản hơn. Khi làm phần code, AI cũng từng gợi ý dùng thư viện hoặc luồng xử lý không còn phù hợp, ví dụ đề xuất `google.generativeai` dù SDK này đã bị cảnh báo deprecated, hoặc giả định API key luôn hợp lệ trong khi thực tế key của tôi có thể bị báo leaked và không dùng được. Điều này cho tôi thấy AI không phải lúc nào cũng hiểu đúng bối cảnh môi trường chạy thật, nên nếu tin hoàn toàn vào câu trả lời của nó thì rất dễ đi sai hướng.

Để sửa những vấn đề đó, tôi đã điều chỉnh cách đặt prompt theo hướng chặt hơn và có ranh giới rõ hơn. Thay vì hỏi chung chung, tôi yêu cầu AI trả lời theo format cụ thể: có bài toán, actor, workflow, bottleneck, metric và architecture. Với phần prompt prototype, tôi bổ sung `SYSTEM_PROMPT` nêu rõ vai trò, nhiệm vụ, giới hạn của mô hình và buộc output phải bắt đầu bằng `[DRAFT_ONLY]` hoặc trả về JSON khi pin dưới ngưỡng an toàn. Tôi cũng thêm `load_dotenv()` để đọc biến môi trường từ file `.env`, tách secret khỏi code, và thêm fallback cục bộ để script vẫn chạy được khi API key bị lỗi hoặc không truy cập được. Từ trải nghiệm này, tôi rút ra rằng AI hữu ích nhất khi được dùng như một thought-partner có kiểm soát: nó giúp tăng tốc ý tưởng, nhưng con người vẫn phải là người đặt luật chơi, kiểm tra tính đúng, và quyết định cuối cùng.
